<Page x:Class="CollegeJournalApp.Views.Pages.GradesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#FAF9F8" FontFamily="Segoe UI" Title="Успеваемость">

    <Page.Resources>
        <Style x:Key="ViewBtn" TargetType="Button">
            <Setter Property="Height"          Value="30"/>
            <Setter Property="Padding"         Value="16,0"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Width"           Value="28"/>
            <Setter Property="Height"          Value="30"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Background"      Value="White"/>
            <Setter Property="BorderBrush"     Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F3F2F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1" Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Успеваемость" FontSize="18" FontWeight="SemiBold" Foreground="#1F1F1F"/>
                    <TextBlock x:Name="TxtSubtitle" FontSize="12" Foreground="#605E5C"
                               VerticalAlignment="Bottom" Margin="10,0,0,3"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnJournal" Content="📋 Журнал"
                            Background="#0078D4" Foreground="White" BorderBrush="#0078D4"
                            Style="{StaticResource ViewBtn}"
                            Click="BtnJournal_Click"/>
                    <Button x:Name="BtnStatement" Content="📄 Ведомость"
                            Background="White" Foreground="#323130" BorderBrush="#D0D0D0"
                            Style="{StaticResource ViewBtn}" Margin="4,0,0,0"
                            Click="BtnStatement_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Панель фильтров -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1" Padding="24,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                <TextBlock Text="Группа:" FontSize="11" Foreground="#605E5C"
                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="CmbGroup" Width="140" Height="30" FontSize="12"
                          Margin="0,0,16,0" SelectionChanged="CmbGroup_Changed"/>

                <TextBlock x:Name="LblSubject" Text="Предмет:" FontSize="11" Foreground="#605E5C"
                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="CmbSubject" Width="210" Height="30" FontSize="12"
                          Margin="0,0,16,0" SelectionChanged="CmbSubject_Changed"/>

                <Button x:Name="BtnPrev" Content="◀" Style="{StaticResource NavBtn}"
                        Click="BtnPrev_Click"/>
                <TextBlock x:Name="TxtMonth" FontSize="13" FontWeight="SemiBold"
                           Foreground="#1F1F1F" VerticalAlignment="Center"
                           Margin="8,0" MinWidth="130" TextAlignment="Center"/>
                <Button x:Name="BtnNext" Content="▶" Style="{StaticResource NavBtn}"
                        Click="BtnNext_Click"/>

                <Button Content="📥 Экспорт" Height="30" Padding="12,0"
                        Background="#107C10" Foreground="White" BorderThickness="0"
                        FontSize="12" Cursor="Hand" Margin="16,0,0,0"
                        Style="{StaticResource ViewBtn}"
                        Click="BtnExport_Click"/>
            </StackPanel>
        </Border>

        <!-- Таблица -->
        <Border Grid.Row="2" Margin="24,12,24,16" Background="White"
                BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid x:Name="MainGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserSortColumns="False"
                      CanUserResizeRows="False"
                      FrozenColumnCount="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      GridLinesVisibility="All"
                      HorizontalGridLinesBrush="#EFEFEF"
                      VerticalGridLinesBrush="#EFEFEF"
                      BorderThickness="0"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFAFA"
                      HeadersVisibility="Column"
                      FontFamily="Segoe UI" FontSize="12"
                      RowHeight="34"
                      SelectionMode="Single"
                      SelectionUnit="Cell"
                      PreviewMouseLeftButtonDown="MainGrid_PreviewMouseLeftButtonDown">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background"                Value="#F3F2F1"/>
                        <Setter Property="Foreground"                Value="#605E5C"/>
                        <Setter Property="FontSize"                  Value="11"/>
                        <Setter Property="FontWeight"                Value="SemiBold"/>
                        <Setter Property="Padding"                   Value="4,8"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="BorderBrush"               Value="#E0E0E0"/>
                        <Setter Property="BorderThickness"           Value="0,0,1,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding"         Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#CCE4F7"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
            </DataGrid>
        </Border>
    </Grid>
</Page>
